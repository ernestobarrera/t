Audio clips for `pocus_cuello.html`.

- Use Reveal audio-slideshow recorder during the presentation to generate clips.
- Keep generated files in this folder for offline playback.
- The deck is configured with `audio.prefix = "assets/pocus_cuello/"` and `audio.suffix = ".webm"`.
